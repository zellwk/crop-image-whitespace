Trims whitespace from images automatically, to the edge of the image.
Automatically crop whitespaces from images.

- Put images into the `images` folder
- Run `node run.js`
- Images will be trimmed and saved into the `out` folder
